# Secrets — never commit (holds PRIVATE_KEY)
.env

# Dependencies
node_modules/

# Runtime state
seen-trades.json
trades-log.json
status.json

# Misc
.DS_Store
npm-debug.log*
updown-signals.ndjson
updown-signals.ndjson.sock
mm-status.json
mm-signals.ndjson
mm-signals.ndjson.1
